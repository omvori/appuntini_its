#include <stdio.h>

int main(){
	int x, y = 4, z;
	x = 1;
	float pi = 3.14;
	
	printf("x = %d, y = %d, pi = %.2f, z = %d\n", x, y, pi, z);	// per z stampa il valore di una casuale cella di memoria nel coomputer perchè non è stato definito
}